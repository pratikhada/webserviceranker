This project ranks a SOAP Based Webservice based different parameters such availability efficiency latency etc and rank it for the user. It takes WSDL from the SOAP service, processes it and rank it.
